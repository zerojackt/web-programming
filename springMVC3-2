make it

jsp page 는 copy 해서 paste 했다

1.글작성하는 page - write_view
2.그 작성한걸 database 에 insert into 해서 넣는 - write

3.글내용을 보는 page 를 만들었다 - select 로 보는 content_view 
+ hit = > view count 는 method 를 따로 만들어줬다 다른 method 와 동일하지만 .. update 를 사용한다는 점
          Hit = Hit +1 를 한다
          

